Some sample input & output files.

Files with a name that starts with "invalid" contains invalid input.

Possible outputs for valid input files are
in the output directory.  They have the same name as the corresponding input
file.
